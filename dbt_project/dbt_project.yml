name: 'hmda_fair_lending'
version: '1.0.0'
config-version: 2

profile: 'hmda_fair_lending'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  hmda_fair_lending:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
